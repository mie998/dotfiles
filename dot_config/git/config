[core]
    autocrlf = false
    ignoreCase = false
    editor = vim

[user]
    email = nishiwaki.kyoto@gmail.com
    name = mie998

[init]
    defaultBranch = main

[status]
    showUntrackedFiles = all

[commit]
    verbose = true
    gpgSign = false # overwritten in conf.d/local.conf

[diff]
    algorithm = histogram

[tag]
    forceSignAnnotated = false # overwritten in conf.d/local.conf

[merge]
    ff = false
    conflictStyle = diff3

[fetch]
    prune = true

[pull]
    ff = only

[push]
    default = simple
    followTags = true

[advice]
    addEmptyPathspec = false

[gpg]
    program = gpg

[include]
    path = conf.d/alias.conf
    path = conf.d/delta.conf
